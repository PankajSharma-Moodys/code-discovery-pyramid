# How To — Benchmarking (`benchmarks/`)

```bash
python3 benchmarks/run_benchmark.py           # graded coverage benchmark
python3 benchmarks/run_live_holdout.py        # live-model lesson A/B
```

See `BENCHMARKS.md` for the full methodology and results.
