# Where To — MCP Server (`mcp_server/`)

| | |
|---|---|
| Core | `mcp_server/tools.py` (`cdp_query`/`cdp_scan`/`cdp_status`), `mcp_server/schemas.py` (`estimate_at_rest_tokens`), `mcp_server/server.py` (`create_server`, `main`) |
| Shared logic | `cdp/query.py` (`dispatch`) |
| Tests | `mcp_server/tests/` |
