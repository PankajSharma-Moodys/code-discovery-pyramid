# How To — Graph Building & Resolution (`graph.py`, `resolve.py`, `dataflow.py`)

No direct CLI surface — inspect via query:

```bash
cdp query symbol X              # resolved import table, transposed
cdp query module m
cdp query paths --from A --to B
```
